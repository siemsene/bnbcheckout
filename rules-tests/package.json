{
  "name": "rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security rules tests for Checkout Rush (standalone workspace)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-checkout \"npx vitest run\"",
    "test:raw": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "vitest": "^3.2.4"
  }
}
