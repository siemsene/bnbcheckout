rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder until Phase 3 — deny all client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
